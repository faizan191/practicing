#include <stdio.h>
#include <stdlib.h>
#define MAX 1000

int main(){
	 int n,i,j;
    scanf("%d", &n);
    for (int i = n; i > 0; --i)
       {
       	for (j = 0; j < i; ++j)
       	{
       		printf("%d",i );
       		
       	}
       	printf("\n");
       }   
    return 0;  	

}
